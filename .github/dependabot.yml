version: 2
updates:
  - package-ecosystem: "bundler" # Ruby gems Jekyll theme package manager
    directory: "/" # Gemfile location
    schedule:
      interval: "monthly" # Check frequency
    ignore:
      - dependency-name: "jekyll" # Optional: Avoid unrelated Jekyll updates
